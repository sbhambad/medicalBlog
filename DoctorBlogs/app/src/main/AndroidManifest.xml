<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anupamsarfare.doctorblogs" >

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activities.Splash"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Home"
            android:label="@string/title_activity_home" />
        <activity
            android:name=".activities.BlogList"
            android:label="@string/title_activity_blog_list" >
        </activity>
        <activity
            android:name=".activities.Categories"
            android:label="@string/title_activity_categories" >
        </activity>
        <activity
            android:name=".activities.BlogDetails"
            android:label="@string/title_activity_blog_details" >
        </activity>
    </application>

</manifest>
